# Architecture Overview
Bitecho is a decentralized signaling network relying on an append-only cryptographic log.

## The Web of Trust (Cryptographic Memory)
Bitecho is a decentralized signaling network relying on an append-only cryptographic log.

## Peer-to-Peer Signaling (WebRTC)
Bitecho is a decentralized signaling network relying on an append-only cryptographic log.

## Proof of Relay (Echos)
Bitecho is a decentralized signaling network relying on an append-only cryptographic log.
